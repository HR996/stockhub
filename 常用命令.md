# 常用命令

本文档整理本项目 PostgreSQL 数据库的连接、查看、迁移、备份和排错命令。

## 数据库连接信息

本项目使用 PostgreSQL 15，可运行在本机系统服务或独立数据库服务器上。

| 项目 | 值 |
| --- | --- |
| 数据库类型 | `PostgreSQL 15` |
| 本机地址 | `localhost:5432` |
| 用户名 | `istock` |
| 密码 | `istock` |
| 数据库名 | `istock` |
| 本地开发 `DATABASE_URL` | `postgresql+psycopg://istock:istock@localhost:5432/istock` |

## 启动和停止数据库

macOS Homebrew：

```bash
brew services start postgresql@15
brew services info postgresql@15
brew services stop postgresql@15
```

## 安装 psql 客户端

安装后可直接执行：

```bash
psql "postgresql://istock:istock@localhost:5432/istock"
```

则需要安装 PostgreSQL 客户端。

### Ubuntu / Debian

```bash
sudo apt update
sudo apt install -y postgresql-client

# 验证安装
psql --version
```

### TencentOS / RHEL / CentOS

本机是 TencentOS Server 3.2，属于 RHEL/CentOS 8 系。项目数据库是 PostgreSQL 15，建议安装同主版本的 `psql` 客户端。

```bash
# 查看可用 PostgreSQL 模块
dnf module list postgresql

# 切换到 PostgreSQL 15 客户端
sudo dnf module reset -y postgresql
sudo dnf module enable -y postgresql:15
sudo dnf install -y postgresql

# 验证安装
psql --version
```

如果已经装过旧版客户端，可以执行：

```bash
sudo dnf module reset -y postgresql
sudo dnf module enable -y postgresql:15
sudo dnf distro-sync -y postgresql
psql --version
```

### macOS

使用 Homebrew：

```bash
brew install libpq
brew link --force libpq

# 验证安装
psql --version
```

也可以安装完整 PostgreSQL：

```bash
brew install postgresql@15
brew link --force postgresql@15
psql --version
```

### Windows

推荐安装 PostgreSQL 官方安装包，安装时勾选 Command Line Tools：

```text
https://www.postgresql.org/download/windows/
```

安装后如果命令行找不到 `psql`，把 PostgreSQL 的 `bin` 目录加入 `PATH`。常见路径类似：

```text
C:\Program Files\PostgreSQL\15\bin
```

重新打开 PowerShell 或 CMD 后验证：

```powershell
psql --version
```

## 连接数据库

```bash
psql "postgresql://istock:istock@localhost:5432/istock"
```

运行一次性命令：

```bash
psql "postgresql://istock:istock@localhost:5432/istock" \
  -c "SELECT current_database(), current_user;"
```

## psql 内常用元命令

以下命令在进入 `psql` 后执行。

```sql
-- 查看所有数据库
\l

-- 连接到 istock 数据库
\c istock

-- 查看当前连接信息
\conninfo

-- 查看所有 schema
\dn

-- 查看 public schema 下的表
\dt public.*

-- 查看表、视图、序列等对象
\d

-- 查看某张表结构
\d stock_basic
\d k_line_daily
\d trade_calendar
\d data_update_task
\d latest_market_cap

-- 查看某张表的详细结构，包括索引、约束等
\d+ stock_basic
\d+ k_line_daily

-- 查看所有索引
\di

-- 查看所有序列
\ds

-- 打开更易读的竖排显示
\x

-- 显示执行耗时
\timing

-- 退出 psql
\q
```

## 本项目当前主要表

当前 Alembic 迁移会创建这些业务表：

| 表名 | 用途 |
| --- | --- |
| `stock_basic` | 股票基础信息 |
| `trade_calendar` | 交易日历 |
| `k_line_daily` | 日 K 线数据 |
| `data_update_task` | 数据更新任务日志 |
| `latest_market_cap` | 最新市值快照 |
| `alembic_version` | Alembic 迁移版本表 |

## 查看库、表、字段和数据量

```sql
-- 查看当前数据库
SELECT current_database();

-- 查看当前用户
SELECT current_user;

-- 查看 public schema 下所有表
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
  AND table_type = 'BASE TABLE'
ORDER BY table_name;

-- 查看每张表的估算行数
SELECT
  schemaname,
  relname AS table_name,
  n_live_tup AS estimated_rows
FROM pg_stat_user_tables
ORDER BY relname;

-- 查看指定表字段
SELECT
  column_name,
  data_type,
  is_nullable,
  column_default
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'stock_basic'
ORDER BY ordinal_position;

-- 查看索引
SELECT
  tablename,
  indexname,
  indexdef
FROM pg_indexes
WHERE schemaname = 'public'
ORDER BY tablename, indexname;

-- 查看外键、唯一约束、主键等约束
SELECT
  conrelid::regclass AS table_name,
  conname AS constraint_name,
  contype AS constraint_type,
  pg_get_constraintdef(oid) AS definition
FROM pg_constraint
WHERE connamespace = 'public'::regnamespace
ORDER BY table_name::text, constraint_name;
```

## 查看业务数据示例

```sql
-- 股票基础信息
SELECT *
FROM stock_basic
ORDER BY ts_code
LIMIT 20;

-- 按市场统计股票数量
SELECT market, COUNT(*) AS count
FROM stock_basic
GROUP BY market
ORDER BY count DESC;

-- 交易日历
SELECT *
FROM trade_calendar
ORDER BY cal_date DESC
LIMIT 20;

-- 最近 K 线数据
SELECT ts_code, trade_date, close_raw, close_qfq, volume, amount
FROM k_line_daily
ORDER BY trade_date DESC, ts_code
LIMIT 50;

-- 查看某只股票 K 线
SELECT *
FROM k_line_daily
WHERE ts_code = '000001.SZ'
ORDER BY trade_date DESC
LIMIT 30;

-- 最新市值快照
SELECT *
FROM latest_market_cap
ORDER BY snapshot_at DESC
LIMIT 20;

-- 数据更新任务日志
SELECT id, task_type, task_key, status, started_at, finished_at, success_count, error_count, created_by
FROM data_update_task
ORDER BY started_at DESC
LIMIT 20;
```

## 一次性查看表清单和行数

在终端执行：

```bash
psql "postgresql://istock:istock@localhost:5432/istock" -c "
SELECT
  schemaname,
  relname AS table_name,
  n_live_tup AS estimated_rows
FROM pg_stat_user_tables
ORDER BY relname;
"
```

## Alembic 迁移命令

项目使用 Alembic 管理数据库 schema，迁移文件在 `backend/alembic/versions/`。

执行迁移前设置数据库地址：

```bash
cd backend
export DATABASE_URL='postgresql+psycopg://istock:istock@localhost:5432/istock'

# 查看当前数据库迁移版本
uv run alembic current

# 升级到最新迁移
uv run alembic upgrade head

# 查看迁移历史
uv run alembic history

# 查看当前 head
uv run alembic heads

# 自动生成迁移，生成后要人工检查
uv run alembic revision --autogenerate -m "describe change"

# 回退一个版本，谨慎使用
uv run alembic downgrade -1
```

## 备份和恢复

备份当前数据库到本地文件：

```bash
pg_dump "postgresql://istock:istock@localhost:5432/istock" > istock_backup.sql
```

从备份恢复：

```bash
psql "postgresql://istock:istock@localhost:5432/istock" < istock_backup.sql
```

备份为自定义格式，适合 `pg_restore`：

```bash
pg_dump "postgresql://istock:istock@localhost:5432/istock" -Fc > istock_backup.dump
```

恢复自定义格式备份：

```bash
pg_restore --dbname="postgresql://istock:istock@localhost:5432/istock" \
  --clean --if-exists istock_backup.dump
```

## 清空或重建本地数据库

只清空业务表数据，保留表结构：

```sql
TRUNCATE TABLE
  latest_market_cap,
  k_line_daily,
  stock_basic,
  trade_calendar,
  data_update_task
RESTART IDENTITY CASCADE;
```

完全删除并重建本地数据库，谨慎使用：

```bash
dropdb istock
createdb -O istock istock
cd backend
export DATABASE_URL='postgresql+psycopg://istock:istock@localhost:5432/istock'
uv run alembic upgrade head
```

## Python 快速连库检查

在 `backend` 目录下执行：

```bash
cd backend
export DATABASE_URL='postgresql+psycopg://istock:istock@localhost:5432/istock'
uv run python - <<'PY'
import psycopg

with psycopg.connect("postgresql://istock:istock@localhost:5432/istock") as conn:
    with conn.cursor() as cur:
        cur.execute("""
            SELECT table_name
            FROM information_schema.tables
            WHERE table_schema = 'public'
            ORDER BY table_name
        """)
        for row in cur.fetchall():
            print(row[0])
PY
```

## 常见排错

### 连接被拒绝

先确认 PostgreSQL 系统服务是否启动：

```bash
brew services info postgresql@15
pg_isready -h localhost -p 5432
```

如果没有启动：

```bash
brew services start postgresql@15
```

Linux 请使用对应服务管理命令，例如 `sudo systemctl status postgresql`。

### 查看当前数据库实际迁移版本

```sql
SELECT * FROM alembic_version;
```

如果表不存在，通常说明还没有执行 Alembic 迁移。

### 执行 `\d+ 表名` 报 `column c.relhasoids does not exist`

这是 `psql` 客户端版本太旧导致的兼容性问题，不是业务表损坏。

典型场景：

```text
psql 客户端：10.x
PostgreSQL 服务端：15.x
```

PostgreSQL 12 以后移除了系统列 `relhasoids`，旧版 `psql` 的 `\d+` 仍会查询它，所以会报错。

解决方式：升级本机 `psql` 到与服务端一致的主版本，例如 PostgreSQL 15。

```bash
psql --version
PGPASSWORD=istock psql "postgresql://istock@localhost:5432/istock" -c "SHOW server_version;"

sudo dnf module reset -y postgresql
sudo dnf module enable -y postgresql:15
sudo dnf distro-sync -y postgresql

psql --version
```

升级后重新进入数据库：

```bash
PGPASSWORD=istock psql "postgresql://istock@localhost:5432/istock"
```

再执行：

```sql
\d+ k_line_daily
\d+ stock_basic
\d+ data_update_task
```
